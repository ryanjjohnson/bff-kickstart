# Normalize line endings for text files across platforms
* text=auto

# Binary assets - never diff or merge as text
*.png binary
*.ico binary
*.pptx binary
*.jar binary

# Generated lockfile: treat conflicts as regenerate-and-commit, not hand-merge
package-lock.json -diff
