<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.example.bffkickstart</groupId>
  <artifactId>backend</artifactId>
  <version>0.1.0</version>
  <!-- war, not jar: produces a deployable WAR for a standalone servlet container
       (Tomcat 11 / Servlet 6.1) while spring-boot-maven-plugin still repackages it as an
       *executable* war, so `java -jar backend.war` (and the container image)
       runs it embedded exactly as before. See ServletInitializer for the WAR
       bootstrap + the standalone-container session-cookie fix. -->
  <packaging>war</packaging>
  <name>bff-kickstart-backend</name>
  <description>BFF Kickstart backend - Spring Boot 3.5 REST + Hibernate + OIDC BFF</description>

  <properties>
    <java.version>25</java.version>
    <!-- Tomcat + Jackson versions come from Boot 4.1.1's own dependency management
         now (Tomcat 11.x / Servlet 6.1, Jackson 3.x) - the earlier 3.5.x CVE
         overrides are dropped because those managed versions are current. -->
  </properties>

  <dependencies>
    <!-- Boot 4 renamed spring-boot-starter-web -> spring-boot-starter-webmvc. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <!-- Embedded Tomcat is `provided`: bundled when running embedded
         (`java -jar` / the container image), but NOT packed into WEB-INF/lib of
         the WAR, so a standalone Tomcat 11 uses its own runtime instead of a
         conflicting embedded copy. Boot 4 splits this out as
         spring-boot-starter-tomcat-runtime (the WAR-deployment variant) - see
         the 4.0 migration guide. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-tomcat-runtime</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-client</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- OpenAPI docs + Swagger UI - gated at runtime by SWAGGER_ENABLED (see application.properties),
         off by default. -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <!-- 3.x is the Spring Boot 4 / Spring Framework 7 line (2.x is Boot 3). -->
      <version>3.0.3</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-csv</artifactId>
      <version>1.14.1</version>
    </dependency>

    <dependency>
      <groupId>com.github.librepdf</groupId>
      <artifactId>openpdf</artifactId>
      <version>1.3.42</version>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
    </dependency>
  </dependencies>

  <build>
    <finalName>backend</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!-- JDK 23+ changed javac's default to -proc:none, so annotation processors found on
               the classpath (Lombok) silently stop running and every generated getter/builder
               becomes "cannot find symbol". Declaring the processor path explicitly restores
               processing on every JDK, old or new. -->
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
